rootProject.name = 'spring-cloud-study'

file('simple-project').eachDir {
    include(":${it.name}")
    project(":${it.name}").projectDir = it
}

file('gateway-ex').eachDir {
    include(":${it.name}")
    project(":${it.name}").projectDir = it
}